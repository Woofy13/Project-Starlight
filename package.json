{
  "name": "project-starlight",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @workspace/bot run dev",
    "start": "pnpm --filter @workspace/bot run start",
    "db:push": "pnpm --filter @workspace/db run push"
  }
}
